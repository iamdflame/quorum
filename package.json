{
  "name": "shoal",
  "private": true,
  "version": "0.1.0",
  "description": "Anonymity aggregation. You do not pick a pool \u2014 you pick to be indistinguishable.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -b packages/oracle packages/router packages/chain packages/execute packages/linkage packages/sdk-bridge",
    "test": "node --test --experimental-strip-types packages/oracle/test/*.test.ts packages/router/test/*.test.ts packages/execute/test/*.test.ts packages/linkage/test/*.test.ts",
    "typecheck": "tsc -b packages/oracle packages/router packages/chain packages/execute packages/linkage packages/sdk-bridge",
    "demo": "npm run build && node examples/the-crowd-you-are-in.mjs",
    "live": "npm run build && node examples/live-mainnet.mjs",
    "pipeline": "npm run build && node examples/pipeline.mjs",
    "linkage": "npm run build && node examples/linkage.mjs",
    "entities": "npm run build && node examples/entities.mjs",
    "test:bridge": "node --test --experimental-strip-types packages/sdk-bridge/test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "starknet": "^10.7.1"
  }
}